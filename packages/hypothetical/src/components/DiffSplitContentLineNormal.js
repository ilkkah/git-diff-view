const { Component } = require('../Component');

class DiffSplitContentLineNormal extends Component {
  constructor(options) {
    super(options);
  }

  render() {
    // Placeholder render method
    return '';
  }
}
module.exports = { DiffSplitContentLineNormal };
