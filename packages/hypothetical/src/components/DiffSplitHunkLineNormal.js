const { Component } = require('../Component');

class DiffSplitHunkLineNormal extends Component {
  constructor(options) {
    super(options);
  }

  render() {
    // Placeholder render method
    return '';
  }
}
module.exports = { DiffSplitHunkLineNormal };
