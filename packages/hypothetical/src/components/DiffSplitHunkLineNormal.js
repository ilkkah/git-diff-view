import { Component } from '../Component';

export class DiffSplitHunkLineNormal extends Component {
  constructor(options) {
    super(options);
  }

  render() {
    // Placeholder render method
    return '';
  }
}
